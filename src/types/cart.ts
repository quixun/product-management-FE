export type CartItem = {
  id: string;
  name: string;
  price: number;
  quantity: number;
};

export type itemSale = {
  productId: string;
  soldById: string;
  pricePerItem: number;
  quantity: number;
};
